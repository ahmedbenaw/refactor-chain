---
description: Something is broken or slow — find the real cause and fix it, safely.
argument-hint: [what's wrong]
allowed-tools: Read, Edit, Write, MultiEdit, Grep, Glob, Bash, Skill, Agent, TodoWrite
---

Use the `refactor-chain` skill in DEBUG mode. Reproduce the problem first, trace it back to the true cause (not just where it shows up), fix it in a small safe step, and prove it's fixed. Tell me what you found in plain words.

What's wrong: $ARGUMENTS
