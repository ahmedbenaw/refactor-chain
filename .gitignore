*.log
.DS_Store
.refactor-chain/
node_modules/

# local-only working notes (never published)
*.local.md

# local-only publish-token cache — NEVER commit (publish.sh reads/writes it, gitignored)
.env
.env.*
