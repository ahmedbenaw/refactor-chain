# Rules Manifest — `<project-name>`

> Generated by `refactor-rules` (refactor-chain). These are the **project's own**
> structural rules, inferred from patterns the code already follows. A human has
> confirmed them. This manifest is the structural contract enforced before and
> after each refactor step.

- **Stack:** `<language / framework from refactor-understand>`
- **Scope:** `<paths covered>`
- **Extracted at:** `<ISO timestamp>`
- **Baseline commit / state:** `<git sha or "working tree">`

## Rules

| ID | Rule (plain English) | Severity | Detection | Conforming | Violating (baseline) |
|----|----------------------|----------|-----------|-----------:|---------------------:|
| `R01` | <e.g. Controllers must not access the data layer directly; go through a service.> | `ERROR` | <how it's found, e.g. "an `@RestController` class references a `*Repository`/DAO type"> | `<n>` | `<n>` |
| `R02` | <e.g. Every service dependency is constructor-injected; no `new ServiceX()`.> | `ERROR` | <detection> | `<n>` | `<n>` |
| `R03` | <e.g. No module under `core/` imports from `web/` (dependencies point inward).> | `ERROR` | <detection> | `<n>` | `<n>` |
| `R04` | <e.g. DTOs live in `dto/` and end in `Dto`.> | `WARNING` | <detection> | `<n>` | `<n>` |
| `R05` | <e.g. Public service methods return a `Result`/`Optional`, never raw `null`.> | `WARNING` | <detection> | `<n>` | `<n>` |
| `R06` | <e.g. Value objects are immutable (`final` fields / `readonly`).> | `SUGGESTION` | <detection> | `<n>` | `<n>` |

> Severity key: `ERROR` = a new one blocks the change · `WARNING` = flagged to the
> review gate · `SUGGESTION` = noted, non-blocking.

## Human confirmation
- Rules kept: `<R01, R02, ...>`
- Rules vetoed / downgraded (with reason): `<e.g. "R04 dropped — team is mid-migration, both DTO styles intended">`

## Enforcement log

### Before (baseline)
```json
{ "R01": 0, "R02": 0, "R03": 0, "R04": 2, "R05": 5, "R06": 8 }
```

### After (post-step: `<step / skill name>`)
```json
{ "R01": 0, "R02": 0, "R03": 0, "R04": 2, "R05": 5, "R06": 8 }
```

### Delta
- **New `ERROR` violations:** `<0 = pass>`
- **New `WARNING`/`SUGGESTION` violations:** `<list, surfaced to review gate>`
- **Result:** `<legal | drift>`
- **Harness signal:** `{ "delta": "<legal|drift>", "newViolations": [ ] }`

## Notes for the reviewer
- <anything the human should know: a rule near its threshold, a known intentional exception, a rule that may need tightening later>
