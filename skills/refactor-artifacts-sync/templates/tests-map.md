# Tests Map — <project name>

> Last synced by refactor-chain: <date> · lane `<lane>` · updated entries: <list>

Maps behaviors/modules to the tests that cover them, so the next person knows what's pinned.

## Coverage by module
| Module / behavior | Test file(s) | Type | Notes |
|---|---|---|---|
| `<module>` | `<test path>` | unit / integration / characterization | <…> |
| `<module>` | `<test path>` | e2e | <…> |

## Baseline & characterization tests
Tests added by `refactor-safety-net` to pin behavior before the refactor:
| Behavior pinned | Test file | Added when |
|---|---|---|
| `<behavior>` | `<path>` | baseline / step `<n>` |

## Known coverage gaps
- <module/behavior with no test> — <risk note> (human prose — preserve)

## How to run
- Full suite: `<command>`
- Single module: `<command>`
